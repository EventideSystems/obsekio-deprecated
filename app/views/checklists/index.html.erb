<% content_for :header do %>
  <div class="xmt-4 xsm:ml-16 xsm:mt-0 xsm:flex-none">
  </div>
<% end %>

<h1 class="sr-only">Library Checklists</h1>
<% if @checklists.empty? %>
  <%= render partial: 'empty_page' %>
<% else %>
  <%= render partial: 'table', locals: { checklists: @checklists } %>
<% end %>
